
{
  "entities": {
    "UserPrompt": {
      "title": "User Prompt History",
      "description": "Stores the original and enhanced prompts for a user.",
      "type": "object",
      "properties": {
        "originalPrompt": {
          "type": "string",
          "description": "The prompt provided by the user."
        },
        "enhancedPrompt": {
          "type": "string",
          "description": "The result generated by the AI."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the prompt was enhanced."
        }
      },
      "required": [
        "originalPrompt",
        "enhancedPrompt",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "/users/{userId}/history/{promptId}": {
      "schema": "UserPrompt",
      "description": "Collection of prompt enhancement history for a specific user."
    }
  }
}
